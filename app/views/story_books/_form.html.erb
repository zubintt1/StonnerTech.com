<%= form_for(story_book) do |f| %>
  <% if story_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(story_book.errors.count, "error") %> prohibited this story_book from being saved:</h2>

      <ul>
      <% story_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
